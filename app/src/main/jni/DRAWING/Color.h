
Color colorByDistance(int distance, float alpha)
{
	Color _colorByDistance;
	if (distance < 500)
		_colorByDistance = Color(44, 0, 0, alpha);
	if (distance < 490)
		_colorByDistance = Color(92, 0, 0, alpha);
	if (distance < 480)
		_colorByDistance = Color(42, 38, 51, alpha);
	if (distance < 470)
		_colorByDistance = Color(116, 38, 0, alpha);
	if (distance < 460)
		_colorByDistance = Color(153, 38, 0, alpha);
	if (distance < 450)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 440)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 430)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 420)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 410)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 400)
		_colorByDistance = Color(153, 173, 0, alpha);
	if (distance < 390)
		_colorByDistance = Color(0, 65, 100, alpha);
	if (distance < 370)
		_colorByDistance = Color(29, 100, 100, alpha);
	if (distance < 350)
		_colorByDistance = Color(100, 100, 100, alpha);
	if (distance < 330)
		_colorByDistance = Color(104, 65, 111, alpha);
	if (distance < 300)
		_colorByDistance = Color(99, 65, 23, alpha);
	if (distance < 290)
		_colorByDistance = Color(58, 91, 0, alpha);
	if (distance < 370)
		_colorByDistance = Color(58, 91, 45, alpha);
	if (distance < 350)
		_colorByDistance = Color(133, 91, 45, alpha);
	if (distance < 330)
		_colorByDistance = Color(133, 91, 45, alpha);
	if (distance < 200)
		_colorByDistance = Color(183, 91, 45, alpha);
	if (distance < 190)
		_colorByDistance = Color(114, 152, 68, alpha);
	if (distance < 180)
		_colorByDistance = Color(152, 111, 51, alpha);
	if (distance < 170)
		_colorByDistance = Color(231, 111, 51, alpha);
	if (distance < 160)
		_colorByDistance = Color(255, 58, 51, alpha);
	if (distance < 150)
		_colorByDistance = Color(255, 75, 51, alpha);
	if (distance < 140)
		_colorByDistance = Color(255, 133, 51, alpha);
	if (distance < 130)
		_colorByDistance = Color(81, 105, 51, alpha);
	if (distance < 120)
		_colorByDistance = Color(103, 109, 112, alpha);
	if (distance < 110)
		_colorByDistance = Color(0, 109, 112, alpha);
	if (distance < 100)
		_colorByDistance = Color(0, 0, 0, alpha);
	if (distance < 90)
		_colorByDistance = Color(0, 0, 0, alpha);
	if (distance < 80)
		_colorByDistance = Color(0, 0, 0, alpha);
	if (distance < 70)
		_colorByDistance = Color(32, 107, 10, alpha);
	if (distance < 60)
		_colorByDistance = Color(197, 75, 0, alpha);
	if (distance < 50)
		_colorByDistance = Color(0, 0, 0, alpha);
	if (distance < 40)
		_colorByDistance = Color(107, 64, 10, alpha);
	if (distance < 30)
		_colorByDistance = Color(112, 34, 10, alpha);
	if (distance < 20)
		_colorByDistance = Color(32, 107, 10, alpha);
	if (distance < 10)
		_colorByDistance = Color(0, 0, 0, alpha);
	return _colorByDistance;
}

Color colorByDistance2(int distance, float alpha)
{
	Color _colorByDistance2;
	if (distance < 500)
		_colorByDistance2 = Color(44, 0, 0, alpha);
	if (distance < 490)
		_colorByDistance2 = Color(92, 0, 0, alpha);
	if (distance < 480)
		_colorByDistance2 = Color(42, 38, 51, alpha);
	if (distance < 470)
		_colorByDistance2 = Color(116, 38, 0, alpha);
	if (distance < 460)
		_colorByDistance2 = Color(153, 38, 0, alpha);
	if (distance < 450)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 440)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 430)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 420)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 410)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 400)
		_colorByDistance2 = Color(153, 173, 0, alpha);
	if (distance < 390)
		_colorByDistance2 = Color(0, 65, 100, alpha);
	if (distance < 370)
		_colorByDistance2 = Color(29, 100, 100, alpha);
	if (distance < 350)
		_colorByDistance2 = Color(100, 100, 100, alpha);
	if (distance < 330)
		_colorByDistance2 = Color(104, 65, 111, alpha);
	if (distance < 300)
		_colorByDistance2 = Color(99, 65, 23, alpha);
	if (distance < 290)
		_colorByDistance2 = Color(58, 91, 0, alpha);
	if (distance < 370)
		_colorByDistance2 = Color(58, 91, 45, alpha);
	if (distance < 350)
		_colorByDistance2 = Color(133, 91, 45, alpha);
	if (distance < 330)
		_colorByDistance2 = Color(133, 91, 45, alpha);
	if (distance < 200)
		_colorByDistance2 = Color(183, 91, 45, alpha);
	if (distance < 190)
		_colorByDistance2 = Color(114, 152, 68, alpha);
	if (distance < 180)
		_colorByDistance2 = Color(152, 111, 51, alpha);
	if (distance < 170)
		_colorByDistance2 = Color(231, 111, 51, alpha);
	if (distance < 160)
		_colorByDistance2 = Color(255, 58, 51, alpha);
	if (distance < 150)
		_colorByDistance2 = Color(255, 75, 51, alpha);
	if (distance < 140)
		_colorByDistance2 = Color(255, 133, 51, alpha);
	if (distance < 130)
		_colorByDistance2 = Color(81, 105, 51, alpha);
	if (distance < 120)
		_colorByDistance2 = Color(103, 109, 112, alpha);
	if (distance < 110)
		_colorByDistance2 = Color(0, 0, 0, alpha);
	if (distance < 100)
		_colorByDistance2 = Color(255, 115, 115, alpha);
	if (distance < 90)
		_colorByDistance2 = Color(0, 255, 255, alpha);
	if (distance < 80)
		_colorByDistance2 = Color(57, 194, 255, alpha);
	if (distance < 70)
		_colorByDistance2 = Color(255, 146, 10, alpha);
	if (distance < 60)
		_colorByDistance2 = Color(255, 0, 255, alpha);
	if (distance < 50)
		_colorByDistance2 = Color(255, 255, 255, alpha);
	if (distance < 40)
		_colorByDistance2 = Color(255, 98, 0, alpha);
	if (distance < 30)
		_colorByDistance2 = Color(255, 0, 0, alpha);
	if (distance < 20)
		_colorByDistance2 = Color(255, 0, 0, alpha);
	if (distance < 10)
		_colorByDistance2 = Color(255, 0, 0, alpha);
	return _colorByDistance2;
}
